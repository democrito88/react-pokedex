import React from 'react';
import { useFetch } from '../hooks/useFetch';

function Cadeia(props){
    let url = Object.values(props.pokemon).map((propriedade) => {
        
    })
    const [species, setSpecies] = useFetch();

}

export default Cadeia;